source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

group :development do
  # deployment tools
  gem 'capistrano'
  gem 'capistrano-yarn'
  gem 'slackistrano'
end

